<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>beast-package</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <!-- version.xml from project root -->
    <files>
        <file>
            <source>${project.basedir}/version.xml</source>
            <outputDirectory>/</outputDirectory>
        </file>
        <!-- Source JAR at top level (BEAST convention: PKG_NAME.src.jar) -->
        <file>
            <source>${project.build.directory}/${project.build.finalName}-sources.jar</source>
            <destName>${beast.pkg.name}.src.jar</destName>
            <outputDirectory>/</outputDirectory>
        </file>
    </files>

    <!-- Module JAR only (BEAST core, JavaFX, and their transitive deps are provided by the runtime) -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <includes>
                <include>io.github.tgvaughan:feast</include>
            </includes>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <!-- fxtemplates/ (JPMS-safe module-namespaced resource path) -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources/sampled.ancestors/fxtemplates</directory>
            <outputDirectory>/fxtemplates</outputDirectory>
        </fileSet>
        <!-- examples/ from project root -->
        <fileSet>
            <directory>${project.basedir}/examples</directory>
            <outputDirectory>/examples</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>